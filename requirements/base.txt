# Versions are left out on purpose in order for you to use the latest ones.
# After the first "pip install -r", just run "pip freeze" and add the version
# to each package in each requirements/*.txt.

Django>=1.10,<1.11

django-model-utils

psycopg2

git+git://github.com/DemocracyClub/dc_base_theme.git@e4dc67ce289f8f93b6dbbdcf568095941d6c5e71
django-formtools
djangorestframework
markdown
django-filter
django_extensions
requests
requests_cache
glob2
